# Dependências Python diretas do CityBot.
# Este arquivo lista os pacotes usados pelo código ativo em `main.py` e `src/`.
# Requisito externo: o Tesseract OCR precisa estar instalado no sistema.
# Observação: `tkinter` faz parte da biblioteca padrão do Python em instalações comuns do Windows.

# Base
beautifulsoup4==4.12.3
pillow==11.3.0
pyperclip==1.9.0
pypdf==5.1.0
python-docx==1.1.2
python-dotenv==1.0.1
requests==2.32.3
youtube-transcript-api==0.6.3

# OCR
langdetect==1.0.9
opencv-python==4.10.0.84
pytesseract==0.3.13

# Provedor Gemini
google-genai==1.67.0

# Provedor Groq
langchain==0.1.20
langchain-groq==0.1.5

# Compatibilidade com scripts legados em `testes/`
langchain-community==0.0.38
